# @tfit/backend

Next.js (App Router) API for TFIT, deployed on Vercel as `tfit-backend`.

See the repo root [`README.md`](../../README.md) and [`docs/`](../../docs) for architecture, database, security, and API conventions. Local setup:

```bash
vercel env pull --yes
npm run dev
```
